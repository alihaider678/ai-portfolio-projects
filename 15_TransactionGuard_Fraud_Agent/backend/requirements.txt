# FastAPI backend for TransactionGuard.
# Self-sufficient (includes the agent deps) so it deploys standalone on Render.
fastapi>=0.139.0
uvicorn[standard]>=0.32.1
starlette>=0.49.1
python-dotenv>=1.0.1

# Agent deps
langgraph>=0.2.60
langchain-core>=0.3.30
openai>=1.40.0
asyncpg>=0.29.0

# Backend-only deps
redis>=5.0.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0
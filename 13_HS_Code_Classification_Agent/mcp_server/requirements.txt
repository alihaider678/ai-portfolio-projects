# TariffLens — HS-code classification engine + MCP server
mcp>=1.2.0
openai>=1.40.0          # embeddings (text-embedding-3-small) + LLM reranker
chromadb>=0.5.0         # local vector store (dense retrieval)
rank-bm25>=0.2.2        # sparse keyword retrieval (hybrid search)
httpx>=0.27.0           # download the USITC HTS dataset in ingest.py
numpy>=1.26.0           # rank fusion / vector math
# FastAPI REST wrapper around the screening engine.
# NOTE: FastAPI must be new enough to allow starlette>=0.49.1 (required by the
# `mcp` SDK's sse-starlette), so we pin modern minimums rather than exact old ones.
fastapi>=0.139.0
uvicorn[standard]>=0.32.1
starlette>=0.49.1
# Screening engine deps (shared with mcp_server/) — listed here too so the
# backend deploys standalone without relying on a cross-directory -r path.
rapidfuzz>=3.9.0
jellyfish>=1.1.0
# Needed by ingest.py, which runs at build time to download the datasets.
httpx>=0.27.0